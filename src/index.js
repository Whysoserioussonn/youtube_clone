import React from "react";
import ReactDOM from "react-dom/client" // append client to react-som this was changed in v18 of react
import App from "./App";

const root =ReactDOM.createRoot(document.getElementById('root'));

root.render(<App />);